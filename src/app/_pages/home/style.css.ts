import { style } from '@vanilla-extract/css';

export const container = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '32px',
  paddingBottom: '80px',
});

export const tagSectionTitle = style({
  fontSize: '2rem',
  fontWeight: 'bold',
  marginBottom: '2rem',
});

export const postSectionTitle = style({
  fontSize: '2rem',
  fontWeight: 'bold',
  marginBottom: '2rem',
});

export const postListContainer = style({});
